# requirements.txt
#
# Install everything with:
#   pip install -r requirements.txt
#
# ── Web framework ─────────────────────────────────────────────────────────────
# fastapi        — Python web framework (like Express for Node.js)
#                  Generates /docs automatically. Used to define our endpoints.
# uvicorn        — ASGI server that runs FastAPI
#                  "uvicorn app.main:app --reload" starts with auto-restart
# python-multipart — Required for FastAPI to handle file uploads
#                   (multipart/form-data). Without this, UploadFile won't work.

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ── Google Gemini ─────────────────────────────────────────────────────────────
# google-genai   — Official Google Generative AI Python SDK
#                  Replaces the old google-generativeai package.
#                  Supports image generation via response_modalities=["IMAGE"].
#                  https://pypi.org/project/google-genai/

google-genai>=0.8.0

# ── Settings / Config ─────────────────────────────────────────────────────────
# pydantic-settings — Loads settings from environment variables and .env files
#                    Works with pydantic v2 (which FastAPI requires)
# python-dotenv    — Reads .env files (used internally by pydantic-settings)

pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# ── Image processing ──────────────────────────────────────────────────────────
# Pillow         — Python Imaging Library
#                  Used for image format detection and any post-processing

Pillow==10.4.0

# ── HuggingFace / IDM-VTON ────────────────────────────────────────────────────
# gradio_client  — HuggingFace Spaces Python client
#                  Used to call IDM-VTON Space: yisol/IDM-VTON
#                  No API key required. Free tier with queue.

gradio_client>=1.3.0

# ── Background Removal ─────────────────────────────────────────────────────────


# --- Computer Vision Pipeline Dependencies ---
opencv-python-headless>=4.10.0
mediapipe>=0.10.14
scikit-image>=0.24.0
numpy>=1.26.4
